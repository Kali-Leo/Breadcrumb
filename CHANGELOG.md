# CHANGELOG

面向 Leo 的变更摘要（中文）。细粒度历史见 git log（Conventional Commits）。

## 2026-09-02 — 全项目安全审计与整理

### 安全

- **供应链钉死了**：CI 和发布流程里每个第三方 action 都钉到 commit SHA，`ci.yml` 的权限收成只读，
  发布多附一份 `SHA256SUMS`（下载完可以自己核对），并加上 dependabot 和 `SECURITY.md`。
- **语言包下载先验摘要**：sha256 随安装包一起发，对不上直接拒绝，然后才轮到内容校验；
  下载也归网络总开关管。构建脚本用的上游语料全部钉在 `upstream.lock.json` 里，还加了大小上限。
- **外链抓取逐跳重检**：改成自己跟随重定向，每一跳都重新过一遍私有地址名单 ——
  以前一个 `302` 就能把本机服务的响应当"证据"读回来。
- **AI 服务地址必须是 https**（本机回环地址除外，那里跑的是 Ollama 这类没有证书的东西）；
  流式回复加了单行和全文长度上限，以及 chunk 之间 30 秒静默就断。
- **提示词边界**：喂给模型的证据摘录用围栏包起来并声明"这不是指令"；同学的反思内容走白名单并限长；
  每个来自模型的字符串都补上了长度上限。
- **窗口不再自己乱跑**：AI 回复里的链接一律交给系统浏览器打开，webview 自己不导航（这个窗口没有
  地址栏，加载进来的页面和应用长得一样），另加一道导航守卫；调试浮层只在开发构建里存在。

### 网页版

- **本地嵌入模型回来了**：和桌面版同一个 `multilingual-e5-small`，在 Web Worker 里用
  transformers.js 跑（q8，与桌面向量余弦 ≥ 0.995）。113 MB 只在第一次真的要用时下，
  归网络总开关管；下载源先探 `huggingface.co` 再退 `hf-mirror.com`，ONNX 运行时的 wasm
  改从本站同源目录发（默认走 jsDelivr，大陆不可直连）。十个降级的功能就此恢复。
- **可以装到桌面 / 断网能开**：加了 PWA。第一次访问把应用外壳（脚本、样式、地图美术、
  SQLite 的 wasm 与 Worker，约 6.3 MB）存下来，之后**服务器不在也能打开**（本机实测：
  杀掉 preview 进程再刷新，聊天界面照常渲染）。24 MB 字体分片、两个职业数据集、mermaid、
  词包、ONNX 运行时都不进预缓存，第一次用到才缓存 —— 首访快，离线能力随着你用它而变完整。
  iOS 上要自己「分享 → 添加到主屏幕」，这也顺带躲开 Safari 七天不用就清数据的计时器。
- **备份与恢复**：设置页可以把整个数据库导出成一个文件，也可以从备份恢复（覆盖式，要确认两次）。
- **语言包改从本站同源目录下载**：GitHub Releases 的资产两跳重定向都不带 CORS 头，
  在浏览器里 100% 装不上；文件逐字节相同，装之前照样核对 SHA-256。
- **首屏又小了一截**：界面文案从「十一种语言全部打进首屏」改成「只加载当前这一种」，
  关键路径 572 KiB → 443 KiB（gzip）；引导页那 3.3 MB 的中英词包改成真要装演示数据时才取。
- **部署体积小了 22 MB**：Vite 顺着 onnxruntime-web 自己的 `new URL()` 多吐了一份 23.6 MB
  的 wasm 副本，运行时根本不用它（用的是同源那份），现在从产物里删掉。
- 霞鹜文楷从 24 MB 的 TTF 换成按 unicode-range 切的 95 片 WOFF2，一个字都没少。
- 发现页在这一版隐藏（它要连本机端口，正是浏览器该拦的事），不再轮询。

### 清理

- 删掉 23 个没人调用的符号、17 条死掉的界面文案键（十一种语言各一份）。
- 删掉 38 个没人用的地图美术文件 —— 1.6 MB，以前每次构建都打进安装包。
- 删掉一张死表和两个没用的索引（迁移 0052），以及十个声明了却从没用过的工作区依赖。
- 浏览器版另外去掉 19 个多余依赖和两个过时脚本。

### 结构

- **`plugin-*` 十五个包改名 `feature-*`**：运行时插件系统这件事正式判死 —— 没有加载器、
  没有动态加载、没有插件市场，那个前缀过去什么也没描述。
- **`lib/` 和 `components/` 按功能分目录**：`lib/` 从 133 个平铺文件分成 14 个目录，
  `components/` 分成 12 个。纯移动，逻辑一行没动。
- **1011 行的 `migrations.ts` 变成一个目录**，按编号分段；一个测试钉住拆分前后的迁移序列
  逐字节相同。`core-db` 的仓储和类型也按表和领域拆开了。
- **新增 `core-time` 和 `core-random`**：「今天是哪一天」和「种子随机」各自只剩一个定义；
  相对门限的系数也收进 `core-vectors` 一个常数 —— 7 份余弦、4 份门限、6 份哈希、
  3 份日期键的拷贝就此消失。
- **数据库里每个 JSON 列都走同一道校验**：20 处裸的 `JSON.parse` 强转和 5 个手写校验器换掉了。

### 顺手修的 bug

- 王国里的席位图按子串匹配，导致 2 档和 3 档共用一张图，有一张图从来没被加载过。
- 一行损坏的数据以前会从"碰巧读到它的那个面板"抛出来；现在在读的地方就地跳过。
- 维度不对的向量以前被截断当好向量用，现在直接丢弃。
- 窗口标题是错的。
- simlab 的兜底客户端自己拼接 URL，现在走和产品同一个校验过的地址装配。

> 这一轮的详细技术全景另有文档：`docs/技术全景.md`（不在本仓库，由别人撰写）。

## 2026-09-01 — 审计八条的裁决落地（spec 061）

- **地图形状不再变**：一座岛的轮廓只由它的身份决定，长大只是整体放大，绝不重画海岸线；
  聚类大陆的身份落在最早的成员上，加成员不换形状、不换名字。位置和大小照旧会变
  （你的裁决：「形状稳定，剩下的不稳定」）。
- **每日来请教的同学换了挑题方式**：从「记得最不牢的优先」改成「这次回想最划得来的优先」
  —— 一年没碰、上次还答错的那种不再排第一（讲不出来只会难受），学过一阵、正开始忘的排前面。
- **「已完成」这个词换掉了**：知识点卡片改说「学过，现在还熟」，跟三层曲线一样是估算口径。
- **界面语言从两种变成十一种**：中、英、西、法、葡、俄、阿、印地、印尼、孟加拉、斯瓦希里。
  系统语言认不出来时，第一屏请你自己选一种，不再默认中文。
- **语言学习不再只有中英**：词包改由通用管线从 Wiktionary + 词频表构建，选了哪对下哪对；
  数据不够的语言对直接不出现。新增 30 题词汇小测（两分钟、可跳过），新词从你已经会的地方开始。
- 顺带清掉了求教者上位之前遗留的一批死代码（旧的复习邀请、「回顾:」会话那套）。
- 依据与决策：`specs/061-audit-decisions/spec.md`、`docs/adr/0034`。

## 2026-08-14（二）— 探索线与站点图落地（spec 040/041）

- **消息树**：对话从线性变成树——从任意一轮"续"出新分支，原来的后续完整保留；重新打开会话永远落在你离开的地方。
- **站点图**：右侧面板即"一线一图"——小圆点连线，实心=走过的知识站、空心=相邻未访、灰=生疏、双圈=换乘（这个知识点在别的对话也出现过）；单击任意站定位到那一轮，「续」从那站开新支线。
- **线路卡**：左侧对话条自动命名为「首站 → 末站」（零 token），按知识主题分组，顶部"正在进行"，底部保留按时间浏览；你手动改过的名字永不被覆盖。
- **换乘**：点双圈站可跳到其他对话中同一知识点的位置——旧知识的衔接从此有了物理形态。
- 依据与决策：`docs/adr/0024`；梳理引擎的 LLM 反思层（主题页自动生成）列为二期。

## 2026-08-14 — 教学机制转入隐形（spec 038 修订 + spec 039 立项）

- 撤除三档讲解模式选择器与"讲解模式记录"卡片（Leo 裁决：教学机制外显 = 居高临下）。契约成为唯一且不具名的行为，直给/引导的路由在对话内部完成。
- 契约修订：对方提出直接讲时**立刻**完整给出，不追问、不拖延。
- 确立教学引擎总纲领：给用户主动学习的体验，效率技巧藏在水面下。
- 立项并落地 spec 039 探索之门与探索图谱：
  - **门**：AI 回复里掌握度中低的概念自动成为可推开的门（维基链接式虚线，每条消息最多 3 扇，学情驱动、零 token）；点开有概率先出猜测卡（织入同款信息增益策略），猜对/接近按一次真实检索计入记忆曲线，猜错零记录零惩罚；弹层内可"展开聊聊"（锚定+预填草稿）。
  - **划词**：选中任意词句浮出"解释一下 / 展开聊聊"，只预填不自动发送。
  - **收线**：右侧面板"本次足迹"删除，改为"探索"视图（当前链条 + 收线按钮）；收线展开六层探索图谱——走过的链、真实知识关系、回头补的前置、没走的联结、相邻未访之地、生疏老朋友，全部符号文本呈现（逻辑清晰度唯一标准），每行可指认库中记录。

## 2026-08-13 — 教学引擎第一期（spec 038）

- **教学契约 v2**：聊天常驻提示词从一句语气契约升级为教学契约——事实类答案先行、原理类讲为什么、一次一步一问、要点核验收尾、错答先认对的部分、结尾留未讲分支。desktop 与 simlab 同源引用 `packages/core-teaching`，不再手工同步。
- **讲解模式切换**：输入框上方新增 自动/直给/引导 三档，一步切换、全局持久化，每档一句平实说明。默认"自动"。
- **学情注入**：锚定知识点时，AI 讲解会拿到该点的记忆保留率与掌握证据（决定从哪讲起）、你偏好的讲解方式；当轮说"没听懂"会触发换讲法降档。全部本地计算，零额外 token。
- **重逢会话**：从遗忘地图发起的重逢，AI 现在知道这是间隔复习——先请你回忆，再确认补正，不再当新话题重讲。
- **讲解模式记录**：反馈实验室新增卡片，平实记录各模式使用轮数（只记录，不评优劣）。
- **simlab 绊线**：新增教学纪律判读（单轮多问、超长回复），进跑批指标与摘要。
- 依据：`docs/research/2026-08-13-教学科学化-有效知识传达调研.md`；决策：`docs/adr/0023`。

## 2026-08-14（三）— 专注模式落地（spec 042）

- **选词/划词即入专注**：普通回复里点标记词、或划任意词句按 Enter，全屏专注窗打开——入口不打断（不猜测）；右侧地铁图只在专注中出现。
- **专注内**：回答里可继续选词（走猜测门，调动兴趣）、可对当前内容另起提问——提问在图上以虚线斜下岔出，回答携带上游各站全部语境；实线词站只带父站语境——响应速度与 token 压缩的来源（新计价环节"专注解释"，独立开关默认开）。
- **退出即留痕**：一张条目卡落进对话流（"刚才就「X」做了一次专注探索（N 站）……"），主对话由此知道你学了什么并优化后续回复；点卡随时回到专注、整图恢复。
- 拆除：常驻右栏、门弹层、划词预填条（皆被专注取代）。
- 走查中抓出并修复：React 开发模式下 Enter 确认被双调用、一次建两个会话。
