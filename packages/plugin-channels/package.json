{
  "name": "@breadcrumb/plugin-channels",
  "version": "0.0.1",
  "description": "Headless external-content channel layer: the candidate-item contract, the channel catalog format and starter catalog, the fetch-discipline layer (conditional GET, size cap, rate limit and daily budget, backoff, User-Agent, data-saver), reachability probing, and the generic RSS/Atom/JSON-Feed adapter.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "feedsmith": "^2.9.6",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
