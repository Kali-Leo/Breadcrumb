{
  "name": "@breadcrumb/plugin-factcheck",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless fact-check plugin: extracts check-worthy claims from a chat round, gathers web evidence, and returns gentle per-claim verdicts.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-llm": "workspace:*",
    "cheerio": "^1.0.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
