{
  "name": "@breadcrumb/plugin-feedback",
  "version": "0.0.1",
  "description": "Headless feedback-lab plugin (spec 035): activity heatmap, small wins, cumulative totals, reunion invites, daily bite, system gauge, settled content and evidence — all computed from existing local data, zero token cost.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "@breadcrumb/plugin-diglot-weave": "workspace:*",
    "@breadcrumb/plugin-memory": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
