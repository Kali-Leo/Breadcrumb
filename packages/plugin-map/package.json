{
  "name": "@breadcrumb/plugin-map",
  "version": "0.0.1",
  "description": "Headless knowledge-map engine: similarity, clustering into places, and deterministic force layout for the ink nautical chart.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "d3-force": "^3.0.0"
  },
  "devDependencies": {
    "@types/d3-force": "^3.0.10",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
