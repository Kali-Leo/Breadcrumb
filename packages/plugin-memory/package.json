{
  "name": "@breadcrumb/plugin-memory",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless memory plugin: FSRS-based retention per knowledge node — the fog engine.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "ts-fsrs": "^5.2.1"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
