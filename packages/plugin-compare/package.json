{
  "name": "@breadcrumb/plugin-compare",
  "version": "0.0.1",
  "description": "Headless comparison-tree plugin (spec 023): evidence-backed profile schema, conservative label/alias matching, overlap-ratio aggregation, and the experimental search-build LLM contract with URL-verification pruning.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "@breadcrumb/core-llm": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
