{
  "name": "@breadcrumb/plugin-town",
  "version": "0.0.1",
  "description": "Headless town generation - faithful TypeScript port of watabou's TownGeneratorOS (GPL-3.0, see THIRD_PARTY_NOTICES).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/plugin-map": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
