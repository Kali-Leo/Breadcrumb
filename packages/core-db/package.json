{
  "name": "@breadcrumb/core-db",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Local-first data layer: typed rows, append-only migrations and hand-written SQL repositories over an injected SqlClient.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^3.2.0"
  }
}
