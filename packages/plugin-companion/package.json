{
  "name": "@breadcrumb/plugin-companion",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless companion-cast plugin (spec 037): Character Card V2 subset schema, the three authored companion cards (Shichimi/Pepper/Cumin, text personas derived from Pepper&Carrot under CC-BY 4.0), and the safety module (manipulation-lexicon gate, crisis detection, break reminders).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-i18n": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
