{
  "name": "@breadcrumb/core-time",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless local-calendar arithmetic: the single answer to \"which day is this\" that the heatmap, the research correlations, the daily trail and the palace layout all have to agree on.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^3.2.0"
  }
}
