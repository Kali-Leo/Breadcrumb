{
  "name": "@breadcrumb/plugin-explore",
  "version": "0.0.1",
  "description": "Explore doors and the exploration atlas: door picking, concept guess-gate policy, grading thresholds, and the six-layer chain recap (spec 039).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "@breadcrumb/core-i18n": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
