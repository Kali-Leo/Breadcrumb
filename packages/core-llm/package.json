{
  "name": "@breadcrumb/core-llm",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless LLM layer: OpenAI-compatible streaming client (Zod-validated) and cost calculation.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
