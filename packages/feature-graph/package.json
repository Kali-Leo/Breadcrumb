{
  "name": "@breadcrumb/feature-graph",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless knowledge-graph module: graphology-backed cycle safety, prerequisite closure and topological order over requires/helps edges, plus embedding-based candidate ranking and the LLM edge-judge contract.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "@breadcrumb/core-llm": "workspace:*",
    "@breadcrumb/core-vectors": "workspace:*",
    "graphology": "^0.26.0",
    "graphology-dag": "^0.4.1",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "typescript": "^7.0.2",
    "vitest": "^3.2.0"
  }
}
