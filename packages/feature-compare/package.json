{
  "name": "@breadcrumb/feature-compare",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless comparison-tree module (spec 023): evidence-backed profile schema, conservative label/alias matching, overlap-ratio aggregation, and the experimental search-build LLM contract with URL-verification pruning.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-llm": "workspace:*",
    "@breadcrumb/feature-knowledge-tree": "workspace:*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
