{
  "name": "@breadcrumb/feature-interest",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless interest module: per-round curiosity/confusion/boredom extraction, time-decayed aggregation, explanation-style ranking, embedding-similarity diffusion, and self-report mastery mapping.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "@breadcrumb/core-llm": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
