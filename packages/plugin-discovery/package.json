{
  "name": "@breadcrumb/plugin-discovery",
  "version": "0.0.1",
  "description": "Headless discovery-feed plugin: interest-weight folding over silent events, positive/negative centroid similarity scoring, Thompson-sampled topic exploration, MMR diversity reranking, and the card-batch/article LLM prompt contracts.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "@breadcrumb/core-llm": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
