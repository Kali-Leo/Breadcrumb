{
  "name": "@breadcrumb/core-ingest",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless import: PDF, Markdown and plain text read into heading-aware blocks, then cut into the parent and child passages retrieval is built on.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "pdfjs-dist": "^6.3.289"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
