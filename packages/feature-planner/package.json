{
  "name": "@breadcrumb/feature-planner",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless planner module: the no-goal frontier() recommendation query, goal-driven gapAndPath() with three route strategies plus coverage, and the LLM goal-mapping contract.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "@breadcrumb/core-llm": "workspace:*",
    "@breadcrumb/feature-graph": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
