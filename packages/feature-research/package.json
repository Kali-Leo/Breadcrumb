{
  "name": "@breadcrumb/feature-research",
  "license": "AGPL-3.0-only",
  "version": "0.0.1",
  "description": "Headless research-task module (spec 036): declarative whitelisted local statistics pushed by vetted institutions — Zod-validated, Ed25519-signed, aggregate-only results shown to the user. Zero token cost, nothing leaves the device in v1.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "@breadcrumb/core-i18n": "workspace:*",
    "@breadcrumb/feature-memory": "workspace:*",
    "@noble/curves": "^2.3.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
