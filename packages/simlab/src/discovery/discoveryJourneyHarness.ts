/**
 * Purpose: one import for the whole discovery journey harness — setup (database, settings, seeded
 * randomness, background drain) and the day loop that drives the app through it.
 * Main exports: everything in discoveryJourneySetup and discoveryJourneyDay.
 */
export * from "./discoveryJourneyDay";
export * from "./discoveryJourneySetup";
