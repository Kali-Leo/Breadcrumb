{
  "name": "@breadcrumb/plugin-graph",
  "version": "0.0.1",
  "description": "Headless knowledge-graph plugin: graphology-backed cycle safety, prerequisite closure and topological order over requires/helps edges, plus embedding-based candidate ranking and the LLM edge-judge contract.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@breadcrumb/core-db": "workspace:*",
    "@breadcrumb/core-llm": "workspace:*",
    "graphology": "^0.26.0",
    "graphology-dag": "^0.4.1",
    "graphology-types": "^0.24.8",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
