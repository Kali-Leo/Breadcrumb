# 记忆宫殿 · 阶段封存快照（2026-07-31）

> Leo 决定暂停地图阶段，先重做后端设计。本文档是无缝重启的锚点：
> 记录已完成状态、未了事项、悬而未决的设计问题、恢复步骤。

## 一、当前已完成（分支 feat/knowledge-map，质量门全绿，32 测试）

- **生成引擎**（packages/plugin-map）：蓝噪声网格 → Azgaar 斑块高度图 →
  Nortantis 板块构造陆地掩膜 → mewo2 水力侵蚀 → 河网提取 → Catmull-Rom
  平滑海岸。确定性（同树同图）与稳定性（旧岛不移动）有专门测试
- **村镇生成器**（packages/plugin-town）：TownGeneratorOS 全量移植（41 文件，
  数值级保真），`generateTown(seed, nPatches)` 可用；当前村庄界面（5 号画廊）
  暂未消费它的数据（见"待办"）
- **渲染**（apps/desktop/src/components/map/）：Laham 官方 3 号主题；
  持久渲染器（纹理串门 bug 已根治）；滚轮离散层级跳转（世界→岛→国→村，
  指哪进哪，每层恰好满窗，零平移）；国座大建筑（官方原画，规格随知识量）；
  村庄=建筑画廊整窗场景（5 号方案）；方形地图贴左+右侧信息栏（悬停显示
  知识簇概览）；墨迹浮现/会话脚印/迷雾/字体（霞鹜文楷）
- **治理**：AGPL-3.0 协议 + THIRD_PARTY_NOTICES + ADR-0004/0005/0006 +
  CLAUDE.md 三条铁律（开源优先/自创图形封禁/体验优先于体积）
- **数据**：`map_place_names` 迁移+仓储（改名覆盖层，user 永远压过 ai）

## 二、悬而未决（重启时最先处理）

1. **岛屿位置无语义**（Leo 已点破）：现为"创建顺序+几何打包"，
   "预防医学"会挨着"JavaScript"。备选路线 A 本地嵌入排布 / B LLM 定位 /
   C 仪式性重排；**Leo 说他另有方案，尚未说出**——重启第一件事是听方案
2. **Leo 的后端新设计**将影响：知识树结构/聚类方式（可能替换 treeShape 的
   输入契约）、可能影响层级映射本身。渲染层与生成引擎通过
   `KnowledgeNodeRow[] → buildWorldModel()` 单点接缝，后端怎么变，
   只需重接这一个入口
3. **村庄画廊的 S 曲线道路**是 AI 自定形状（已向 Leo 坦白）；
   已获认可的修正方向：改用 plugin-town 原版代码生成的街道折线
4. 数据量太少导致许多设计无法评估（Leo 原话）——重启后需要造更丰满的
   演示数据集或等真实数据积累
5. 未做：标签避让、AI 简介接入（信息栏已留槽）、AI 改名建议链路、
   跨档生长 toast、Nortantis 海浪/做旧毛边移植（算法笔记在 ADR-0006 会话
   记录中）、性能计时验收

## 三、已确认的产品决定（重启时不要推翻）

- Laham（Nortantis 官方 3 号）为全图主题；村庄=5 号建筑画廊（立面视角，
  Leo 明确不要俯视图）
- 滚轮=层级跳转、每层满窗、零平移；单村国度直入村庄
- 地图上不要河流/山/树/海面装饰（信息干净优先）
- 国界虚线 + 疆域面积∝知识占比（已线性加权）
- 全部视觉元素必须可指认官方开源出处（封禁令）；建筑=Nortantis 官方手绘 PNG
  （这是 Nortantis 官方机制，非 AI 生成贴图）

## 四、恢复步骤

1. `pnpm install && pnpm typecheck && pnpm lint && pnpm test`（应全绿）
2. `cd apps/desktop && pnpm tauri dev`；侧栏「🏛️ 记忆宫殿」
3. DEV 键：0=演示数据集切换，1/2/3/4=层级跳转；滚轮上=深入、下=返回
4. 窗口自动化注意：桌面可能有同名"Breadcrumb"窗口（文件管理器），
   必须 `xdotool search --pid $(pgrep -f breadcrumb-desktop)` 按 PID 选窗
5. 读 memory（自动加载）：应移尽移原则、自创图形封禁令、共用电脑约定
6. 演示对照板存于 Leo 桌面：官方案例板 / 村庄案例板 / 四层级对照板

## 五、与 fact-check 分支（dev-2）的合并说明

dev-2（事实核查功能）与本分支同日并行开发，编号冲突已在合并时处理：
其 spec 008→009、ADR 0004→0007、迁移 0005_factcheck→0006_factcheck。
两分支均已合入 main（详见 PR 记录）。
