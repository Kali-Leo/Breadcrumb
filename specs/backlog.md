# Backlog（未排期的已确认方向）

> 从对话与验收中沉淀的待办，按里程碑认领后移入对应 spec。

- **i18n 脚手架**（Leo 2026-07-29 确认面向全球用户）：把界面文案集中到 messages 模块，
  首发中文、英文其次；代码注释保持英文，不做"注释语言包"（理由：翻译注释会失同步；
  AI 可读代码 + 各自的 AI 助手即是全球开发者的母语通道）
- **缓存命中计价**：DeepSeek 输入 token 分 cache hit/miss 两档，当前按 miss 上界计费；
  后续读取 usage 明细（prompt_tokens_details）做精确计价
- **自定义模型单价 UI**：pricing 引擎已支持覆盖，设置页缺输入界面
- **会话管理**：重命名 / 删除会话
- **消息 Markdown 渲染**：当前纯文本；学习场景需要代码块与公式渲染
- **属性测试引入**（fast-check）：SSE 解析器是好靶子
